
@model IEnumerable<Harmonizer.Core.Model.Association>
@{
    WebGrid webGrid = new WebGrid(source: Model, canPage: false, canSort: false);
}
@if (Model.Count() > 0)
{
    @*<div class="row scrollable-table">
        <div class="col-md-12">
            @webGrid.GetHtml(
                   htmlAttributes: new { @id = "AssociationDetails", @class = "table table-bordered table-sm table-striped search-replace-table sticky-header" },
                   columns: webGrid.Columns(
                       webGrid.Column("FHNumber", format: @<span>@Html.Label("lblFHnumber", String.Format("{0}", (string)item.FHnumber), new { @id = "lblFHnumber" })</span>),
                               webGrid.Column("Associate", format: @<span>@Html.Label("lblAssociate", String.Format("{0}", (string)item.Associate), new { @id = "lblAssociate" })</span>),
                                      webGrid.Column("OriginalDateofAssoc", header: "OriginalDateofAssoc", format: (item) => string.Format("{0:dd-MMM-yyyy}", item.OriginalDateofAssoc)),
                                      webGrid.Column("AssocCanceledDate", header: "AssocCanceledDate", format: (item) => string.Format("{0:dd-MMM-yyyy}", item.AssocCanceledDate)),
                                       webGrid.Column("AssocCanceledBy", format: @<span>@Html.Label("lblAssocCanceledBy", String.Format("{0}", (string)item.AssocCanceledBy))</span>),
                                        //webGrid.Column("AssocStatus", format: @<span>@Html.Label("lblAssocStatus", String.Format("{0}", (bool)item.AssocStatus))</span>),
                                        webGrid.Column("AssocStatus", format: @<span>@Html.Label("lblAssocStatus", item.AssocStatus ? "Active" : "In-Active", new { @id = "lblAssocStatus" })</span>),
                                        webGrid.Column("Update", format: @<input type="button" name='btnRename' value="Update" class="btn-info btn-rename" />))
                                        )

                                </div>
    </div>*@
}
else
{
    <div class="row">
        <div class="col-md-12">
            <span class="text-danger">User not maintain.</span>
        </div>
    </div>
}